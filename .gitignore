# .gitignore (root of terraform directory)

# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include override files you do wish to add to version control using negation pattern
# !example_override.tf

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
*tfplan*

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Lambda function packages
*.zip
lambda/**/*.zip
functions/**/*.zip

# Node modules (if using Node.js for Lambda)
**/node_modules/


# Python virtual environments (if using Python for Lambda)
**/.venv/
**/venv/
**/__pycache__/
**/*.pyc
**/*.pyo
**/*.pyd

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Backup files
*.backup
*.bak

# Lock files (optional - some teams commit this)
# .terraform.lock.hcl

# Environment variables
.env
.env.local
.env.*.local

# Terraform plan output
plan.out
tfplan.out

apps/backend/package/